function numRegions(graph) {
    let visted = new Set();
    let counter = 0;
     
    for(let key in graph) {
        let 
        if(!visted.has(key)) {
            counter++;
        }
    }

}


module.exports = {
    numRegions
};